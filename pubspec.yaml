name: doggy_dogs_car_alarm
description: A virtual pet guard dog that protects your car - Transform car security into an emotionally engaging experience
version: 0.1.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  animations: ^2.0.11
  audioplayers: ^6.1.0
  crypto: ^3.0.3
  flutter:
    sdk: flutter
  flutter_local_notifications: ^18.0.1
  flutter_riverpod: ^2.5.1
  flutter_svg: ^2.0.10+1
  geolocator: ^14.0.0
  intl: ^0.19.0
  lottie: ^3.1.2
  package_info_plus: ^8.0.0
  path_provider: ^2.1.4
  permission_handler: ^11.3.1
  sensors_plus: ^6.0.1
  shared_preferences: ^2.3.2
  sqflite: ^2.3.3+1
  uuid: ^4.5.1
  workmanager: ^0.9.0

dev_dependencies:
  build_runner: ^2.4.13
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  mockito: ^5.4.4

flutter:
  uses-material-design: true

  assets:
    # Dog breed artwork
    - assets/dogs/
    # Sound effects
    - assets/sounds/

  # fonts:
  #   - family: CustomFont
  #     fonts:
  #       - asset: fonts/CustomFont-Regular.ttf
  #       - asset: fonts/CustomFont-Bold.ttf
  #         weight: 700
